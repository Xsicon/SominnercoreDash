@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            // Redirect to sign-in page after component has rendered
            Navigation.NavigateTo("/signin", replace: true);
        }
    }
}